<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/stock.php");
	include_once("{$currDir}/stock_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('stock');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'stock';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Company stock quantity */" => "company_stock_quantity",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product name */" => "product_name",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "product_id",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Supplier */" => "supplier",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Stock quantity */" => "stock_quantity",
		"`stock`.`id`" => "id",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`products1`.`id`',
		2 => '`products1`.`id`',
		3 => '`products1`.`id`',
		4 => '`products1`.`id`',
		5 => '`products1`.`id`',
		6 => '`stock`.`id`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Company stock quantity */" => "company_stock_quantity",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product name */" => "product_name",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "product_id",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Supplier */" => "supplier",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Stock quantity */" => "stock_quantity",
		"`stock`.`id`" => "id",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Company stock quantity */" => "Company stock quantity",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product name */" => "Product name",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "Product id",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Supplier */" => "Supplier",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Stock quantity */" => "Stock quantity",
		"`stock`.`id`" => "ID",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Company stock quantity */" => "company_stock_quantity",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product name */" => "product_name",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "product_id",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Supplier */" => "supplier",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Stock quantity */" => "stock_quantity",
		"`stock`.`id`" => "id",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['product_name' => 'Product name', ];

	$x->QueryFrom = "`stock` LEFT JOIN `products` as products1 ON `products1`.`id`=`stock`.`product_name` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'stock_view.php';
	$x->RedirectAfterInsert = 'stock_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Stock';
	$x->TableIcon = 'resources/table_icons/Database-icon.png';
	$x->PrimaryKey = '`stock`.`id`';

	$x->ColWidth = [150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Company stock quantity', 'Product name', 'Product id', 'Supplier', 'Stock quantity', ];
	$x->ColFieldName = ['company_stock_quantity', 'product_name', 'product_id', 'supplier', 'stock_quantity', ];
	$x->ColNumber  = [1, 2, 3, 4, 5, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/stock_templateTV.html';
	$x->SelectedTemplate = 'templates/stock_templateTVS.html';
	$x->TemplateDV = 'templates/stock_templateDV.html';
	$x->TemplateDVP = 'templates/stock_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `stock`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='stock' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `stock`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='stock' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`stock`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: stock_init
	$render = true;
	if(function_exists('stock_init')) {
		$args = [];
		$render = stock_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: stock_header
	$headerCode = '';
	if(function_exists('stock_header')) {
		$args = [];
		$headerCode = stock_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: stock_footer
	$footerCode = '';
	if(function_exists('stock_footer')) {
		$args = [];
		$footerCode = stock_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
