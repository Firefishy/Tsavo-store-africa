<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/agent.php");
	include_once("{$currDir}/agent_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('agent');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'agent';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`agent`.`first_name`" => "first_name",
		"`agent`.`last_name`" => "last_name",
		"`agent`.`email`" => "email",
		"CONCAT_WS('-', LEFT(`agent`.`phone`,3), MID(`agent`.`phone`,4,3), RIGHT(`agent`.`phone`,4))" => "phone",
		"`agent`.`address`" => "address",
		"`agent`.`id`" => "id",
		"`agent`.`station`" => "station",
		"`agent`.`zone`" => "zone",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => 1,
		2 => 2,
		3 => 3,
		4 => '`agent`.`phone`',
		5 => 5,
		6 => 6,
		7 => 7,
		8 => 8,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`agent`.`first_name`" => "first_name",
		"`agent`.`last_name`" => "last_name",
		"`agent`.`email`" => "email",
		"CONCAT_WS('-', LEFT(`agent`.`phone`,3), MID(`agent`.`phone`,4,3), RIGHT(`agent`.`phone`,4))" => "phone",
		"`agent`.`address`" => "address",
		"`agent`.`id`" => "id",
		"`agent`.`station`" => "station",
		"`agent`.`zone`" => "zone",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`agent`.`first_name`" => "First name",
		"`agent`.`last_name`" => "Last name",
		"`agent`.`email`" => "Email",
		"`agent`.`phone`" => "Phone",
		"`agent`.`address`" => "Address",
		"`agent`.`id`" => "ID",
		"`agent`.`station`" => "Station",
		"`agent`.`zone`" => "Zone",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`agent`.`first_name`" => "first_name",
		"`agent`.`last_name`" => "last_name",
		"`agent`.`email`" => "email",
		"CONCAT_WS('-', LEFT(`agent`.`phone`,3), MID(`agent`.`phone`,4,3), RIGHT(`agent`.`phone`,4))" => "phone",
		"`agent`.`address`" => "address",
		"`agent`.`id`" => "id",
		"`agent`.`station`" => "station",
		"`agent`.`zone`" => "zone",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`agent` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'agent_view.php';
	$x->RedirectAfterInsert = 'agent_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Tsavo Agent';
	$x->TableIcon = 'resources/table_icons/Users-Worker-icon.png';
	$x->PrimaryKey = '`agent`.`id`';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['First name', 'Last name', 'Email', 'Phone', 'Address', 'Station', 'Zone', ];
	$x->ColFieldName = ['first_name', 'last_name', 'email', 'phone', 'address', 'station', 'zone', ];
	$x->ColNumber  = [1, 2, 3, 4, 5, 7, 8, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/agent_templateTV.html';
	$x->SelectedTemplate = 'templates/agent_templateTVS.html';
	$x->TemplateDV = 'templates/agent_templateDV.html';
	$x->TemplateDVP = 'templates/agent_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `agent`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='agent' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `agent`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='agent' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`agent`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: agent_init
	$render = true;
	if(function_exists('agent_init')) {
		$args = [];
		$render = agent_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: agent_header
	$headerCode = '';
	if(function_exists('agent_header')) {
		$args = [];
		$headerCode = agent_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: agent_footer
	$footerCode = '';
	if(function_exists('agent_footer')) {
		$args = [];
		$footerCode = agent_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
