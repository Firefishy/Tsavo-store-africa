<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("$currDir/lib.php");

	handle_maintenance();

	header('Content-type: text/javascript; charset=' . datalist_db_encoding);

	$table_perms = getTablePermissions('supplier');
	if(!$table_perms['access']) die('// Access denied!');

	$mfk = $_GET['mfk'];
	$id = makeSafe($_GET['id']);
	$rnd1 = intval($_GET['rnd1']); if(!$rnd1) $rnd1 = '';

	if(!$mfk) {
		die('// No js code available!');
	}

	switch($mfk) {

		case 'drop_off_point':
			if(!$id) {
				?>
				$j('#drop_off_contact<?php echo $rnd1; ?>').html('&nbsp;');
				<?php
				break;
			}
			$res = sql("SELECT `drop_off`.`id` as 'id', `drop_off`.`point_name` as 'point_name', `drop_off`.`location` as 'location', `drop_off`.`licence` as 'licence', `drop_off`.`packages_accepted` as 'packages_accepted', `drop_off`.`operating_licence` as 'operating_licence', `drop_off`.`station_email` as 'station_email', `drop_off`.`station_photo` as 'station_photo', `drop_off`.`station_contact` as 'station_contact', `drop_off`.`agent_incharge` as 'agent_incharge', `drop_off`.`agent_id` as 'agent_id', if(`drop_off`.`station_reg_date`,date_format(`drop_off`.`station_reg_date`,'%m/%d/%Y'),'') as 'station_reg_date' FROM `drop_off`  WHERE `drop_off`.`id`='{$id}' limit 1", $eo);
			$row = db_fetch_assoc($res);
			?>
			$j('#drop_off_contact<?php echo $rnd1; ?>').html('<?php echo addslashes(str_replace(array("\r", "\n"), '', nl2br($row['station_contact']))); ?>&nbsp;');
			<?php
			break;


	}

?>