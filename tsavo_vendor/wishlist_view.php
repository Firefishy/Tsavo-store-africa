<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/wishlist.php");
	include_once("{$currDir}/wishlist_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('wishlist');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'wishlist';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`wishlist`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "user_id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`), CONCAT_WS('',   `users1`.`firstname`), '') /* Wishlist First Name */" => "w_fname",
		"IF(    CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`lastname`), '') /* Wishlist Lastname */" => "w_lname",
		"IF(    CHAR_LENGTH(`users1`.`email`), CONCAT_WS('',   `users1`.`email`), '') /* Wishlist Email */" => "w_email",
		"IF(    CHAR_LENGTH(`users1`.`contact_info`), CONCAT_WS('',   `users1`.`contact_info`), '') /* Wishlist Phone Number */" => "w_pnumber",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "product_id",
		"IF(    CHAR_LENGTH(`products1`.`name`), CONCAT_WS('',   `products1`.`name`), '') /* product in the Wishlist */" => "w_product",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`wishlist`.`id`',
		2 => '`users1`.`id`',
		3 => '`users1`.`firstname`',
		4 => '`users1`.`lastname`',
		5 => '`users1`.`email`',
		6 => '`users1`.`contact_info`',
		7 => '`products1`.`id`',
		8 => '`products1`.`name`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`wishlist`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "user_id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`), CONCAT_WS('',   `users1`.`firstname`), '') /* Wishlist First Name */" => "w_fname",
		"IF(    CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`lastname`), '') /* Wishlist Lastname */" => "w_lname",
		"IF(    CHAR_LENGTH(`users1`.`email`), CONCAT_WS('',   `users1`.`email`), '') /* Wishlist Email */" => "w_email",
		"IF(    CHAR_LENGTH(`users1`.`contact_info`), CONCAT_WS('',   `users1`.`contact_info`), '') /* Wishlist Phone Number */" => "w_pnumber",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "product_id",
		"IF(    CHAR_LENGTH(`products1`.`name`), CONCAT_WS('',   `products1`.`name`), '') /* product in the Wishlist */" => "w_product",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`wishlist`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "User id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`), CONCAT_WS('',   `users1`.`firstname`), '') /* Wishlist First Name */" => "Wishlist First Name",
		"IF(    CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`lastname`), '') /* Wishlist Lastname */" => "Wishlist Lastname",
		"IF(    CHAR_LENGTH(`users1`.`email`), CONCAT_WS('',   `users1`.`email`), '') /* Wishlist Email */" => "Wishlist Email",
		"IF(    CHAR_LENGTH(`users1`.`contact_info`), CONCAT_WS('',   `users1`.`contact_info`), '') /* Wishlist Phone Number */" => "Wishlist Phone Number",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "Product id",
		"IF(    CHAR_LENGTH(`products1`.`name`), CONCAT_WS('',   `products1`.`name`), '') /* product in the Wishlist */" => "product in the Wishlist",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`wishlist`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "user_id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`), CONCAT_WS('',   `users1`.`firstname`), '') /* Wishlist First Name */" => "w_fname",
		"IF(    CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`lastname`), '') /* Wishlist Lastname */" => "w_lname",
		"IF(    CHAR_LENGTH(`users1`.`email`), CONCAT_WS('',   `users1`.`email`), '') /* Wishlist Email */" => "w_email",
		"IF(    CHAR_LENGTH(`users1`.`contact_info`), CONCAT_WS('',   `users1`.`contact_info`), '') /* Wishlist Phone Number */" => "w_pnumber",
		"IF(    CHAR_LENGTH(`products1`.`id`), CONCAT_WS('',   `products1`.`id`), '') /* Product id */" => "product_id",
		"IF(    CHAR_LENGTH(`products1`.`name`), CONCAT_WS('',   `products1`.`name`), '') /* product in the Wishlist */" => "w_product",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['user_id' => 'User id', 'product_id' => 'Product id', ];

	$x->QueryFrom = "`wishlist` LEFT JOIN `users` as users1 ON `users1`.`id`=`wishlist`.`user_id` LEFT JOIN `products` as products1 ON `products1`.`id`=`wishlist`.`product_id` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'wishlist_view.php';
	$x->RedirectAfterInsert = 'wishlist_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Wishlist';
	$x->TableIcon = 'resources/table_icons/card_gift_2.png';
	$x->PrimaryKey = '`wishlist`.`id`';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['User id', 'Wishlist First Name', 'Wishlist Lastname', 'Wishlist Email', 'Wishlist Phone Number', 'Product id', 'product in the Wishlist', ];
	$x->ColFieldName = ['user_id', 'w_fname', 'w_lname', 'w_email', 'w_pnumber', 'product_id', 'w_product', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, 7, 8, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/wishlist_templateTV.html';
	$x->SelectedTemplate = 'templates/wishlist_templateTVS.html';
	$x->TemplateDV = 'templates/wishlist_templateDV.html';
	$x->TemplateDVP = 'templates/wishlist_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `wishlist`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='wishlist' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `wishlist`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='wishlist' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`wishlist`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: wishlist_init
	$render = true;
	if(function_exists('wishlist_init')) {
		$args = [];
		$render = wishlist_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: wishlist_header
	$headerCode = '';
	if(function_exists('wishlist_header')) {
		$args = [];
		$headerCode = wishlist_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: wishlist_footer
	$footerCode = '';
	if(function_exists('wishlist_footer')) {
		$args = [];
		$footerCode = wishlist_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
