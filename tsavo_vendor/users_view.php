<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/users.php");
	include_once("{$currDir}/users_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('users');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'users';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`users`.`id`" => "id",
		"`users`.`wallet`" => "wallet",
		"`users`.`password`" => "password",
		"`users`.`type`" => "type",
		"`users`.`firstname`" => "firstname",
		"`users`.`lastname`" => "lastname",
		"`users`.`email`" => "email",
		"`users`.`contact_info`" => "contact_info",
		"`users`.`address`" => "address",
		"`users`.`photo`" => "photo",
		"`users`.`status`" => "status",
		"`users`.`activate_code`" => "activate_code",
		"`users`.`reset_code`" => "reset_code",
		"if(`users`.`created_on`,date_format(`users`.`created_on`,'%m/%d/%Y'),'')" => "created_on",
		"`users`.`zone`" => "zone",
		"IF(    CHAR_LENGTH(`coupon1`.`coupon_code`) || CHAR_LENGTH(`coupon1`.`coupon_id`), CONCAT_WS('',   `coupon1`.`coupon_code`, `coupon1`.`coupon_id`), '') /* Redeem Code */" => "discount",
		"`users`.`coupon_code`" => "coupon_code",
		"`users`.`coupon_worth`" => "coupon_worth",
		"`users`.`cost`" => "cost",
		"IF(    CHAR_LENGTH(`coupon2`.`coupon_id`), CONCAT_WS('',   `coupon2`.`coupon_id`), '') /* Coupon id */" => "coupon_id",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`users`.`id`',
		2 => 2,
		3 => 3,
		4 => '`users`.`type`',
		5 => 5,
		6 => 6,
		7 => 7,
		8 => 8,
		9 => 9,
		10 => 10,
		11 => '`users`.`status`',
		12 => 12,
		13 => 13,
		14 => '`users`.`created_on`',
		15 => '`users`.`zone`',
		16 => 16,
		17 => '`users`.`coupon_code`',
		18 => 18,
		19 => '`users`.`cost`',
		20 => '`coupon2`.`coupon_id`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`users`.`id`" => "id",
		"`users`.`wallet`" => "wallet",
		"`users`.`password`" => "password",
		"`users`.`type`" => "type",
		"`users`.`firstname`" => "firstname",
		"`users`.`lastname`" => "lastname",
		"`users`.`email`" => "email",
		"`users`.`contact_info`" => "contact_info",
		"`users`.`address`" => "address",
		"`users`.`photo`" => "photo",
		"`users`.`status`" => "status",
		"`users`.`activate_code`" => "activate_code",
		"`users`.`reset_code`" => "reset_code",
		"if(`users`.`created_on`,date_format(`users`.`created_on`,'%m/%d/%Y'),'')" => "created_on",
		"`users`.`zone`" => "zone",
		"IF(    CHAR_LENGTH(`coupon1`.`coupon_code`) || CHAR_LENGTH(`coupon1`.`coupon_id`), CONCAT_WS('',   `coupon1`.`coupon_code`, `coupon1`.`coupon_id`), '') /* Redeem Code */" => "discount",
		"`users`.`coupon_code`" => "coupon_code",
		"`users`.`coupon_worth`" => "coupon_worth",
		"`users`.`cost`" => "cost",
		"IF(    CHAR_LENGTH(`coupon2`.`coupon_id`), CONCAT_WS('',   `coupon2`.`coupon_id`), '') /* Coupon id */" => "coupon_id",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`users`.`id`" => "Id",
		"`users`.`wallet`" => "Wallet",
		"`users`.`password`" => "Password",
		"`users`.`type`" => "Type",
		"`users`.`firstname`" => "Firstname",
		"`users`.`lastname`" => "Lastname",
		"`users`.`email`" => "Email",
		"`users`.`contact_info`" => "Contact_info",
		"`users`.`address`" => "Address",
		"`users`.`status`" => "Status",
		"`users`.`activate_code`" => "Activate_code",
		"`users`.`reset_code`" => "Reset_code",
		"`users`.`created_on`" => "Created_on",
		"`users`.`zone`" => "Zone",
		"IF(    CHAR_LENGTH(`coupon1`.`coupon_code`) || CHAR_LENGTH(`coupon1`.`coupon_id`), CONCAT_WS('',   `coupon1`.`coupon_code`, `coupon1`.`coupon_id`), '') /* Redeem Code */" => "Redeem Code",
		"`users`.`coupon_code`" => "Coupon Code",
		"`users`.`coupon_worth`" => "Coupon worth",
		"`users`.`cost`" => "Cost",
		"IF(    CHAR_LENGTH(`coupon2`.`coupon_id`), CONCAT_WS('',   `coupon2`.`coupon_id`), '') /* Coupon id */" => "Coupon id",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`users`.`id`" => "id",
		"`users`.`wallet`" => "wallet",
		"`users`.`password`" => "password",
		"`users`.`type`" => "type",
		"`users`.`firstname`" => "firstname",
		"`users`.`lastname`" => "lastname",
		"`users`.`email`" => "email",
		"`users`.`contact_info`" => "contact_info",
		"`users`.`address`" => "address",
		"`users`.`status`" => "status",
		"`users`.`activate_code`" => "activate_code",
		"`users`.`reset_code`" => "reset_code",
		"if(`users`.`created_on`,date_format(`users`.`created_on`,'%m/%d/%Y'),'')" => "created_on",
		"`users`.`zone`" => "zone",
		"IF(    CHAR_LENGTH(`coupon1`.`coupon_code`) || CHAR_LENGTH(`coupon1`.`coupon_id`), CONCAT_WS('',   `coupon1`.`coupon_code`, `coupon1`.`coupon_id`), '') /* Redeem Code */" => "discount",
		"`users`.`coupon_code`" => "coupon_code",
		"`users`.`coupon_worth`" => "coupon_worth",
		"`users`.`cost`" => "cost",
		"IF(    CHAR_LENGTH(`coupon2`.`coupon_id`), CONCAT_WS('',   `coupon2`.`coupon_id`), '') /* Coupon id */" => "coupon_id",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['discount' => 'Redeem Code', 'coupon_id' => 'Coupon id', ];

	$x->QueryFrom = "`users` LEFT JOIN `coupon` as coupon1 ON `coupon1`.`coupon_id`=`users`.`discount` LEFT JOIN `coupon` as coupon2 ON `coupon2`.`coupon_id`=`users`.`coupon_id` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'users_view.php';
	$x->RedirectAfterInsert = 'users_view.php';
	$x->TableTitle = 'Users/Members';
	$x->TableIcon = 'resources/table_icons/users-icon.png';
	$x->PrimaryKey = '`users`.`id`';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, 150, 150, 150, 150, 150, 150, 80, 150, ];
	$x->ColCaption = ['Id', 'Wallet', 'Firstname', 'Lastname', 'Email', 'Contact_info', 'Address', 'Photo', 'Created_on', 'Zone', 'Redeem Code', 'Coupon Code', 'Coupon worth', 'Cost', 'Coupon id', ];
	$x->ColFieldName = ['id', 'wallet', 'firstname', 'lastname', 'email', 'contact_info', 'address', 'photo', 'created_on', 'zone', 'discount', 'coupon_code', 'coupon_worth', 'cost', 'coupon_id', ];
	$x->ColNumber  = [1, 2, 5, 6, 7, 8, 9, 10, 14, 15, 16, 17, 18, 19, 20, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/users_templateTV.html';
	$x->SelectedTemplate = 'templates/users_templateTVS.html';
	$x->TemplateDV = 'templates/users_templateDV.html';
	$x->TemplateDVP = 'templates/users_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = true;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `users`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='users' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `users`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='users' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`users`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: users_init
	$render = true;
	if(function_exists('users_init')) {
		$args = [];
		$render = users_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: users_header
	$headerCode = '';
	if(function_exists('users_header')) {
		$args = [];
		$headerCode = users_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: users_footer
	$footerCode = '';
	if(function_exists('users_footer')) {
		$args = [];
		$footerCode = users_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
