<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/male_pants.php");
	include_once("{$currDir}/male_pants_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('male_pants');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'male_pants';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`male_pants`.`id`" => "id",
		"`male_pants`.`name`" => "name",
		"`male_pants`.`cat_slug`" => "cat_slug",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`male_pants`.`id`',
		2 => 2,
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`male_pants`.`id`" => "id",
		"`male_pants`.`name`" => "name",
		"`male_pants`.`cat_slug`" => "cat_slug",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`male_pants`.`id`" => "ID",
		"`male_pants`.`name`" => "Name",
		"`male_pants`.`cat_slug`" => "Cat slug",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`male_pants`.`id`" => "id",
		"`male_pants`.`name`" => "name",
		"`male_pants`.`cat_slug`" => "cat_slug",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`male_pants` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'male_pants_view.php';
	$x->RedirectAfterInsert = 'male_pants_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Male pants';
	$x->TableIcon = 'resources/table_icons/check-icon.png';
	$x->PrimaryKey = '`male_pants`.`id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Name', 'Cat slug', ];
	$x->ColFieldName = ['name', 'cat_slug', ];
	$x->ColNumber  = [2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/male_pants_templateTV.html';
	$x->SelectedTemplate = 'templates/male_pants_templateTVS.html';
	$x->TemplateDV = 'templates/male_pants_templateDV.html';
	$x->TemplateDVP = 'templates/male_pants_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `male_pants`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='male_pants' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `male_pants`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='male_pants' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`male_pants`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: male_pants_init
	$render = true;
	if(function_exists('male_pants_init')) {
		$args = [];
		$render = male_pants_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: male_pants_header
	$headerCode = '';
	if(function_exists('male_pants_header')) {
		$args = [];
		$headerCode = male_pants_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: male_pants_footer
	$footerCode = '';
	if(function_exists('male_pants_footer')) {
		$args = [];
		$footerCode = male_pants_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
