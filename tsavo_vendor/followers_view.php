<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/followers.php");
	include_once("{$currDir}/followers_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('followers');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'followers';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`followers`.`likeid`" => "likeid",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* Follower id */" => "userid",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '.  .', `users1`.`lastname`), '') /* Follower names */" => "f_fname",
		"`followers`.`vendorid`" => "vendorid",
		"if(`followers`.`follow_date`,date_format(`followers`.`follow_date`,'%m/%d/%Y'),'')" => "follow_date",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`followers`.`likeid`',
		2 => '`users1`.`id`',
		3 => 3,
		4 => 4,
		5 => '`followers`.`follow_date`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`followers`.`likeid`" => "likeid",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* Follower id */" => "userid",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '.  .', `users1`.`lastname`), '') /* Follower names */" => "f_fname",
		"`followers`.`vendorid`" => "vendorid",
		"if(`followers`.`follow_date`,date_format(`followers`.`follow_date`,'%m/%d/%Y'),'')" => "follow_date",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`followers`.`likeid`" => "Likeid",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* Follower id */" => "Follower id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '.  .', `users1`.`lastname`), '') /* Follower names */" => "Follower names",
		"`followers`.`vendorid`" => "My Shop id",
		"`followers`.`follow_date`" => "Follow date",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`followers`.`likeid`" => "likeid",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* Follower id */" => "userid",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '.  .', `users1`.`lastname`), '') /* Follower names */" => "f_fname",
		"`followers`.`vendorid`" => "vendorid",
		"if(`followers`.`follow_date`,date_format(`followers`.`follow_date`,'%m/%d/%Y'),'')" => "follow_date",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['userid' => 'Follower id', ];

	$x->QueryFrom = "`followers` LEFT JOIN `users` as users1 ON `users1`.`id`=`followers`.`userid` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'followers_view.php';
	$x->RedirectAfterInsert = 'followers_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Followers';
	$x->TableIcon = 'resources/table_icons/group.png';
	$x->PrimaryKey = '`followers`.`likeid`';

	$x->ColWidth = [150, 150, 150, 150, ];
	$x->ColCaption = ['Follower id', 'Follower names', 'My Shop id', 'Follow date', ];
	$x->ColFieldName = ['userid', 'f_fname', 'vendorid', 'follow_date', ];
	$x->ColNumber  = [2, 3, 4, 5, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/followers_templateTV.html';
	$x->SelectedTemplate = 'templates/followers_templateTVS.html';
	$x->TemplateDV = 'templates/followers_templateDV.html';
	$x->TemplateDVP = 'templates/followers_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `followers`.`likeid`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='followers' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `followers`.`likeid`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='followers' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`followers`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: followers_init
	$render = true;
	if(function_exists('followers_init')) {
		$args = [];
		$render = followers_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: followers_header
	$headerCode = '';
	if(function_exists('followers_header')) {
		$args = [];
		$headerCode = followers_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: followers_footer
	$footerCode = '';
	if(function_exists('followers_footer')) {
		$args = [];
		$footerCode = followers_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
