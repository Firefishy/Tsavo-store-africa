<?php
// This script and data application were generated by AppGini 5.96
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/message.php");
	include_once("{$currDir}/message_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('message');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'message';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`message`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "user_id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '  |||   ', `users1`.`lastname`), '') /* User name */" => "user_name",
		"`message`.`product`" => "product",
		"`message`.`message`" => "message",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`message`.`id`',
		2 => '`users1`.`id`',
		3 => 3,
		4 => 4,
		5 => 5,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`message`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "user_id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '  |||   ', `users1`.`lastname`), '') /* User name */" => "user_name",
		"`message`.`product`" => "product",
		"`message`.`message`" => "message",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`message`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "User id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '  |||   ', `users1`.`lastname`), '') /* User name */" => "User name",
		"`message`.`product`" => "Subject",
		"`message`.`message`" => "Message",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`message`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`id`), CONCAT_WS('',   `users1`.`id`), '') /* User id */" => "user_id",
		"IF(    CHAR_LENGTH(`users1`.`firstname`) || CHAR_LENGTH(`users1`.`lastname`), CONCAT_WS('',   `users1`.`firstname`, '  |||   ', `users1`.`lastname`), '') /* User name */" => "user_name",
		"`message`.`product`" => "product",
		"`message`.`message`" => "message",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['user_id' => 'User id', ];

	$x->QueryFrom = "`message` LEFT JOIN `users` as users1 ON `users1`.`id`=`message`.`user_id` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'message_view.php';
	$x->RedirectAfterInsert = 'message_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Message';
	$x->TableIcon = 'resources/table_icons/printer-icon.png';
	$x->PrimaryKey = '`message`.`id`';

	$x->ColWidth = [150, 150, 150, 150, ];
	$x->ColCaption = ['User id', 'User name', 'Subject', 'Message', ];
	$x->ColFieldName = ['user_id', 'user_name', 'product', 'message', ];
	$x->ColNumber  = [2, 3, 4, 5, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/message_templateTV.html';
	$x->SelectedTemplate = 'templates/message_templateTVS.html';
	$x->TemplateDV = 'templates/message_templateDV.html';
	$x->TemplateDVP = 'templates/message_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `message`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='message' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `message`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='message' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`message`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: message_init
	$render = true;
	if(function_exists('message_init')) {
		$args = [];
		$render = message_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: message_header
	$headerCode = '';
	if(function_exists('message_header')) {
		$args = [];
		$headerCode = message_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: message_footer
	$footerCode = '';
	if(function_exists('message_footer')) {
		$args = [];
		$footerCode = message_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
